fun main() {
// if-else statements
    val num1 = 10
    val num2 = 20
// normal if else staement without storing the value what shoes inside if or else
    /* if (num1 > num2) {
         println("$num1 is greater than $num2")
     }
     else{
         println("$num1 is lesser than $num2")
     }*/


// this is when we have to store the value of if and else when run
    /* val result=if (num1 > num2) {
         "$num1 is greater than $num2"
     }
     else{
         "$num1 is lesser than $num2"
     }
    */

// we can not use single else but we can use if also u can not just use the store value method when you have only if statement

// the same above expression in single line
/*val result= if(num1>num2)"$num1 is greater than $num2" else "$num1 is lesser than $num2"
    print(result)*/

// if-else-if ladder
/*val n=-5
    val result=if(n>0){
        "$n is positive"
    }else if(n<0){
        "$n is negative"
    }else{
        "$n is 0"
    }
    print(result)*/

//nestd if else
//when u have if inside if or else if inside if or else then it will be known as nested if



}